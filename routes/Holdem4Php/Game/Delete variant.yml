info:
  name: Delete variant
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/game/delete_variant/1"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
