info:
  name: Delete rule
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/game/delete_rule/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
