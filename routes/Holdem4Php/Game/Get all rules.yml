info:
  name: Get all rules
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/game/get_all_rules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
