info:
  name: Get all variants
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/game/get_all_variants"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
